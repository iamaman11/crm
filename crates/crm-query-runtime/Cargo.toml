[package]
name = "crm-query-runtime"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
path = "src/query_lib.rs"

[dependencies]
base64 = "0.22"
crm-capability-runtime = { path = "../crm-capability-runtime" }
crm-module-sdk = { path = "../crm-module-sdk" }
hmac = "0.12"
sha2 = "0.10"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
