[package]
name = "crm-core-events"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
crm-core-contracts = { path = "../crm-core-contracts" }
crm-module-sdk = { path = "../crm-module-sdk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
