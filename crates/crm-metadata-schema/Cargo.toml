[package]
name = "crm-metadata-schema"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
crm-metadata-runtime = { path = "../crm-metadata-runtime" }
crm-module-sdk = { path = "../crm-module-sdk" }
semver = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
