diff --git a/Cargo.toml b/Cargo.toml
index 5817ce6..769fb59 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -35,11 +35,15 @@ members = [
   "crates/crm-sales-capability-adapter",
   "crates/crm-parties-capability-adapter",
   "crates/crm-parties-query-adapter",
+  "crates/crm-party-reference-composition",
   "crates/crm-customer-accounts-capability-adapter",
+  "crates/crm-customer-accounts-capability-composition",
   "crates/crm-customer-accounts-query-adapter",
   "crates/crm-contact-points-capability-adapter",
+  "crates/crm-contact-points-capability-composition",
   "crates/crm-contact-points-query-adapter",
   "crates/crm-party-relationships-capability-adapter",
+  "crates/crm-party-relationships-capability-composition",
   "crates/crm-party-relationships-query-adapter",
   "crates/crm-party-relationships-projection",
   "crates/crm-consents-capability-adapter",
diff --git a/crates/crm-application-composition/src/lib.rs b/crates/crm-application-composition/src/lib.rs
index be6830b..193c427 100644
--- a/crates/crm-application-composition/src/lib.rs
+++ b/crates/crm-application-composition/src/lib.rs
@@ -96,6 +96,96 @@ impl ModuleRuntimeContribution {
     }
 }
 
+
+/// Collects route fragments by their declared owner and emits one deterministic
+/// contribution per module. Production composition roots use this when module
+/// constructors become available in dependency order while preserving the
+/// invariant that `ApplicationCompositionBuilder` sees each module exactly once.
+#[derive(Default)]
+pub struct ModuleContributionSet {
+    modules: BTreeMap<String, ModuleRuntimeContribution>,
+}
+
+impl fmt::Debug for ModuleContributionSet {
+    fn fmt(&self, formatter: &mut fmt::Formatter<'_>) -> fmt::Result {
+        formatter
+            .debug_struct("ModuleContributionSet")
+            .field("module_ids", &self.modules.keys().collect::<Vec<_>>())
+            .finish()
+    }
+}
+
+impl ModuleContributionSet {
+    pub fn new() -> Self {
+        Self::default()
+    }
+
+    pub fn add_mutations(
+        &mut self,
+        definitions: impl IntoIterator<Item = CapabilityDefinition>,
+        validator: Arc<dyn CapabilitySemanticValidator>,
+        executor: Arc<dyn TransactionalCapabilityExecutor>,
+    ) -> Result<&mut Self, CompositionError> {
+        for definition in definitions {
+            let module_id = definition.owner_module_id.clone();
+            let key = module_id.as_str().to_owned();
+            let contribution = self
+                .modules
+                .entry(key)
+                .or_insert_with(|| ModuleRuntimeContribution::new(module_id));
+            contribution.mutations.push(MutationRoute {
+                definition,
+                validator: validator.clone(),
+                executor: executor.clone(),
+            });
+        }
+        Ok(self)
+    }
+
+    pub fn add_queries(
+        &mut self,
+        definitions: impl IntoIterator<Item = CapabilityDefinition>,
+        validator: Arc<dyn QuerySemanticValidator>,
+        executor: Arc<dyn QueryExecutor>,
+    ) -> Result<&mut Self, CompositionError> {
+        for definition in definitions {
+            let module_id = definition.owner_module_id.clone();
+            let key = module_id.as_str().to_owned();
+            let contribution = self
+                .modules
+                .entry(key)
+                .or_insert_with(|| ModuleRuntimeContribution::new(module_id));
+            contribution.queries.push(QueryRoute {
+                definition,
+                validator: validator.clone(),
+                executor: executor.clone(),
+            });
+        }
+        Ok(self)
+    }
+
+    pub fn add_empty_module(
+        &mut self,
+        module_id: ModuleId,
+    ) -> Result<&mut Self, CompositionError> {
+        let key = module_id.as_str().to_owned();
+        if self.modules.contains_key(&key) {
+            return Ok(self);
+        }
+        self.modules
+            .insert(key, ModuleRuntimeContribution::new(module_id));
+        Ok(self)
+    }
+
+    pub fn build(self) -> Result<ApplicationComposition, CompositionError> {
+        let mut builder = ApplicationCompositionBuilder::new();
+        for contribution in self.modules.into_values() {
+            builder.add_module(contribution)?;
+        }
+        builder.build()
+    }
+}
+
 #[derive(Debug, Clone, PartialEq, Eq)]
 pub enum CompositionError {
     DuplicateModule(String),
@@ -964,6 +1054,31 @@ mod tests {
         assert_eq!(*order.lock().unwrap(), vec!["alpha/a", "zeta/b"]);
     }
 
+
+    #[test]
+    fn contribution_set_merges_route_fragments_without_duplicate_module_registration() {
+        let calls = Arc::new(Mutex::new(Vec::new()));
+        let handler = Arc::new(MutationHandler {
+            calls: Arc::clone(&calls),
+        });
+        let mut set = ModuleContributionSet::new();
+        set.add_mutations(
+            [definition("crm.alpha", "alpha.record.create", true)],
+            handler.clone(),
+            handler.clone(),
+        )
+        .unwrap();
+        set.add_mutations(
+            [definition("crm.alpha", "alpha.record.update", true)],
+            handler.clone(),
+            handler,
+        )
+        .unwrap();
+        let composition = set.build().unwrap();
+        assert_eq!(composition.module_ids(), &BTreeSet::from(["crm.alpha".to_owned()]));
+        assert_eq!(composition.mutation_definitions().len(), 2);
+    }
+
     #[test]
     fn duplicate_and_owner_mismatched_routes_fail_assembly() {
         let handler = Arc::new(MutationHandler {
diff --git a/crates/crm-application-runtime/Cargo.toml b/crates/crm-application-runtime/Cargo.toml
index 598bedf..816361f 100644
--- a/crates/crm-application-runtime/Cargo.toml
+++ b/crates/crm-application-runtime/Cargo.toml
@@ -20,12 +20,14 @@ crm-consents-capability-adapter = { path = "../crm-consents-capability-adapter"
 crm-consents-capability-composition = { path = "../crm-consents-capability-composition" }
 crm-consents-query-adapter = { path = "../crm-consents-query-adapter" }
 crm-contact-points-capability-adapter = { path = "../crm-contact-points-capability-adapter" }
+crm-contact-points-capability-composition = { path = "../crm-contact-points-capability-composition" }
 crm-contact-points-query-adapter = { path = "../crm-contact-points-query-adapter" }
 crm-core-data = { path = "../crm-core-data" }
 crm-core-events = { path = "../crm-core-events" }
 crm-customer-360-composition = { path = "../crm-customer-360-composition" }
 crm-customer-360-query-adapter = { path = "../crm-customer-360-query-adapter" }
 crm-customer-accounts-capability-adapter = { path = "../crm-customer-accounts-capability-adapter" }
+crm-customer-accounts-capability-composition = { path = "../crm-customer-accounts-capability-composition" }
 crm-customer-accounts-query-adapter = { path = "../crm-customer-accounts-query-adapter" }
 crm-customer-data-operations-capability-adapter = { path = "../crm-customer-data-operations-capability-adapter" }
 crm-customer-data-operations-execution-composition = { path = "../crm-customer-data-operations-execution-composition" }
@@ -42,11 +44,12 @@ crm-identity-resolution-query-adapter = { path = "../crm-identity-resolution-que
 crm-identity-resolution-merge-query-adapter = { path = "../crm-identity-resolution-merge-query-adapter" }
 crm-metadata-api-adapter = { path = "../crm-metadata-api-adapter" }
 crm-metadata-query-adapter = { path = "../crm-metadata-query-adapter" }
-crm-module-registry = { path = "../crm-module-registry" }
 crm-module-sdk = { path = "../crm-module-sdk" }
 crm-parties-capability-adapter = { path = "../crm-parties-capability-adapter" }
 crm-parties-query-adapter = { path = "../crm-parties-query-adapter" }
+crm-party-reference-composition = { path = "../crm-party-reference-composition" }
 crm-party-relationships-capability-adapter = { path = "../crm-party-relationships-capability-adapter" }
+crm-party-relationships-capability-composition = { path = "../crm-party-relationships-capability-composition" }
 crm-party-relationships-projection = { path = "../crm-party-relationships-projection" }
 crm-party-relationships-query-adapter = { path = "../crm-party-relationships-query-adapter" }
 crm-proto-contracts = { path = "../crm-proto-contracts" }
@@ -58,7 +61,6 @@ crm-search-query-adapter = { path = "../crm-search-query-adapter" }
 crm-search-runtime = { path = "../crm-search-runtime" }
 http = "1"
 prost = "0.14"
-semver = "1"
 serde = { version = "1", features = ["derive"] }
 serde_json = "1"
 sha2 = "0.10"
diff --git a/crates/crm-application-runtime/src/data_quality_capability_execution.rs b/crates/crm-application-runtime/src/data_quality_capability_execution.rs
index 48810ac..b495520 100644
--- a/crates/crm-application-runtime/src/data_quality_capability_execution.rs
+++ b/crates/crm-application-runtime/src/data_quality_capability_execution.rs
@@ -1,14 +1,10 @@
-use crate::governed_metadata::ApplicationCapabilityExecutorRouter as BaseApplicationCapabilityExecutorRouter;
 use crm_capability_adapters::semantic_input_hash;
 use crm_capability_plan_support as support;
 use crm_capability_runtime::{
     CapabilityAuthorizer, CapabilityDefinition, CapabilityExecutionResult, CapabilityRequest,
     TransactionalCapabilityExecutor,
 };
-use crm_core_data::{
-    PostgresDataStore, PostgresMetadataCapabilityExecutor, PostgresTransactionalAggregateExecutor,
-    RecordGetQuery,
-};
+use crm_core_data::{PostgresDataStore, PostgresTransactionalAggregateExecutor, RecordGetQuery};
 use crm_data_quality_capability_adapter::{
     DataQualityCompletenessProfileCapabilityPlanner, DataQualityEvaluationJobCapabilityPlanner,
     DataQualityRemediationCompletionPlanner, FINDING_RECORD_TYPE, MODULE_ID,
@@ -44,34 +40,23 @@ use std::sync::atomic::{AtomicBool, Ordering};
 static REMEDIATION_FAILPOINT_USED: AtomicBool = AtomicBool::new(false);
 
 #[derive(Clone)]
