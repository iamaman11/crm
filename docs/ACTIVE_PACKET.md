<!-- Generated by scripts/generate_repository_navigation.py; do not edit. -->
<!-- schema: crm.repository-navigation/v1; source-digest: sha256:93dd2811e67bb8cca4f05e4ddea8a2d2473e84fad534071ef618fb592b2b55c6 -->

# Active Repository Packet

- **Packet:** `repository-step-16-worker-contention` — Prove real worker contention converges exactly once
- **Status:** `active`
- **Tracking:** #126, #194
- **Baseline:** `main` / `6f82de0a7b2dcd1ab5dd0ae6473d46d7d9d34bdd`
- **Source digest:** `sha256:93dd2811e67bb8cca4f05e4ddea8a2d2473e84fad534071ef618fb592b2b55c6`

## Objective

Close the remaining implementation gap in Repository Step 16 by forcing two real crm-api Customer Data Operations executors to compete for the same durable retryable Party-import work. Hold the first real Party target insert behind a PostgreSQL advisory barrier, mechanically observe one direct Party waiter plus a second executor transitively blocked in the same database blocking chain, and prove the existing job, idempotency, transaction, audit and checkpoint boundaries converge to exactly one committed effect with no duplicate replay. Do not add a generic lease API or change production worker algorithms. Final Step 16 closure and Step 17 activation remain blocked on a separate accepted evidence-synchronization packet.

## Deliverables

- `extend the accepted retryable import process representative with two simultaneously live recovered crm-api executors`
- `hold the first real Party target insert behind one PostgreSQL advisory-lock barrier and mechanically observe one direct Party waiter plus at least two executor sessions in the same transitive blocking chain`
- `prove the existing idempotency and transaction boundaries serialize the competing executor before any second Party target effect`
- `prove the competing executors converge to one completed checkpoint and exactly one Party record, idempotency record, event and audit record`
- `prove post-contention scheduler cycles add no duplicate target effects`
- `reuse the existing Import Retryable Process Runtime and Generic Mutation Query Conformance gates without adding or changing workflows`
- `regenerate docs/ACTIVE_PACKET.md and synchronize packet/navigation/architecture guards to the exact Step 16 contention baseline`

## Allowed paths

- `docs/ACTIVE_PACKET.md`
- `repository-packet.json`
- `services/crm-api/tests/import_process_retryable_e2e.rs`
- `tests/test_architecture_documentation_consistency.py`
- `tests/test_generic_conformance_adoption.py`
- `tests/test_repository_navigation.py`

## Forbidden paths

- `.github/workflows/**`
- `Cargo.lock`
- `Cargo.toml`
- `apps/**`
- `contracts/**`
- `crates/**`
- `database/**`
- `modules/**`
- `packages/**`
- `proto/**`
- `schemas/**`
- `scripts/**`
- `services/crm-api/src/**`
- `services/crm-api/tests/import_process_e2e.rs`
- `services/crm-api/tests/support/**`

## Required checks

- Affected Scope CI
- Complexity Baseline CI
- Generic Mutation Query Conformance CI
- Governance CI
- Import Retryable Process Runtime CI
- Rust Generated Sync
- Rust CI

## Acceptance

- the branch is based exactly on main commit 6f82de0a7b2dcd1ab5dd0ae6473d46d7d9d34bdd and contains only the six declared bounded Step 16 contention files
- the original durable retryable failure still preserves checkpoint zero and the Party target-effect baseline before restart
- two independent production crm-api processes are simultaneously ready against the same database and import job
- PostgreSQL pg_stat_activity and pg_blocking_pids prove one direct crm.records Party insert waiter and at least two real executor sessions in the same transitive blocking chain rooted at the test advisory barrier
- the blocking chain proves the competing executor is serialized by existing transactional or idempotency ownership before a second Party target effect can occur
- after release the import job reaches one completed checkpoint and exactly one Party record, idempotency record, event and audit record beyond baseline
- the competing loser settles without an additional target effect and later scheduler cycles leave all four target-effect counts unchanged
- no production algorithm, worker API, route, contract, schema, migration, crate, dependency, manifest, Cargo.lock or permanent workflow changes
- one unchanged meaningful user-authored head passes every applicable permanent workflow and final changed-file/thread review
- Repository Step 16 implementation may be accepted through this slice, but Step 16 is not declared complete and Step 17 is not started until separate evidence synchronization is merged

## Explicit non-goals

- introduce a generic lease abstraction, distributed lock service or new worker scheduler semantics
- change Customer Data Operations or Parties production source, persistence, capability or authorization behavior
- add a Customer Privacy worker or change the zero-worker Customer Privacy inventory
- add a new permanent workflow, crate, dependency, manifest, public route, contract, migration or persistence schema
- implement contract lifecycle Step 17, local lifecycle Step 18, Customer Privacy worker Step 19, frontend or operations work
- declare Repository Step 16 fully complete, Phase 8A complete, Architecture 10/10 or any product-complete expert module before separate evidence synchronization

This generated file is orientation only. `repository-packet.json` and the normative architecture plan remain authoritative.
