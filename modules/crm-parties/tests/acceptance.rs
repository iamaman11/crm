use crm_parties::{CRATE_NAME, MODULE_ID};

#[test]
#[ignore = "foundation gate: replace with governed Party production acceptance in Phase 8A.2"]
fn production_acceptance_todo() {
    assert_eq!(CRATE_NAME, "crm-parties");
    assert_eq!(MODULE_ID, "crm.parties");
    panic!("replace foundation placeholder with governed Party production acceptance");
}
