# Identity Resolution module migrations

Phase 8A.5 initially persists candidate cases through the platform's governed generic record store. Module-local SQL migrations remain intentionally empty until a module-owned schema is justified by a later bounded packet.
