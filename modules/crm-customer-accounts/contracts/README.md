# Published contracts for `crm.customer-accounts`

The canonical cross-owner Account identity is `crm.customer.v1.AccountRef` from `proto/crm/customer/v1/reference.proto`.

Public Account behavior contracts are intentionally deferred until the 8A.3 aggregate boundary is implemented and reviewed. Do not invent ad-hoc JSON or duplicate Account wire types inside downstream modules.
