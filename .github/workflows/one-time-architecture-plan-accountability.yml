name: One-time Architecture Plan Accountability

on:
  pull_request:
    branches:
      - main

concurrency:
  group: one-time-architecture-plan-accountability-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.13"
      - name: Install validation dependencies
        run: python -m pip install --requirement requirements-dev.txt
      - name: Materialize exact architecture plan corrections
        run: |
          python scripts/one_time_architecture_plan_accountability_runner.py
          python scripts/generate_repository_navigation.py --write
      - name: Verify permanent documentation and navigation guards
        run: |
          python scripts/generate_repository_navigation.py --check
          python scripts/check_architecture.py
          python -m unittest \
            tests.test_repository_navigation \
            tests.test_architecture_documentation_consistency
      - name: Commit permanent architecture plan corrections
        env:
          HEAD_BRANCH: ${{ github.event.pull_request.head.ref }}
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            docs/ACTIVE_PACKET.md \
            docs/ARCHITECTURE_COMPLEXITY_AND_SCALABILITY_PLAN.md \
            docs/IMPLEMENTATION_ROADMAP.md \
            docs/MODULE_CATALOG.md \
            docs/PHASE8_DELIVERY_PLAN.md \
            docs/PROJECT_STATUS.md \
            tests/test_architecture_documentation_consistency.py \
            tests/test_repository_navigation.py
          git diff --cached --quiet && {
            echo "Expected architecture plan corrections were not produced" >&2
            exit 1
          }
          git commit -m "Docs: clarify stage accountability and 10/10 completion"
          git pull --rebase origin "${HEAD_BRANCH}"
          git push origin "HEAD:${HEAD_BRANCH}"
