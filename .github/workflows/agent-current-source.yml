name: Agent Current Source Snapshot

on:
  pull_request:
    paths:
      - ".github/workflows/agent-current-source.yml"

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.head_ref }}
      - name: Create exact current source snapshot
        run: tar --exclude=.git -czf /tmp/crm-current-source.tgz .
      - uses: actions/upload-artifact@v4
        with:
          name: crm-current-source-${{ github.sha }}
          path: /tmp/crm-current-source.tgz
          if-no-files-found: error
          retention-days: 1
