name: Product Plane CI

on:
  push:
    paths:
      - "package.json"
      - "requirements-dev.txt"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "tsconfig.base.json"
      - "buf.gen.web.yaml"
      - "apps/web/**"
      - "packages/client/**"
      - "packages/ui/**"
      - "contracts/module-contract-bindings.json"
      - "modules/*/module.yaml"
      - "schemas/module.schema.json"
      - "scripts/contract_bindings.py"
      - "scripts/generate_contract_bindings.py"
      - ".github/workflows/product-plane.yml"
      - "scripts/run_e2e.sh"
      - "proto/**"
      - "crates/crm-application-runtime/**"
      - "crates/crm-proto-contracts/**"
      - "services/crm-api/**"
      - "database/**"
  pull_request:
    paths:
      - "package.json"
      - "requirements-dev.txt"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "tsconfig.base.json"
      - "buf.gen.web.yaml"
      - "apps/web/**"
      - "packages/client/**"
      - "packages/ui/**"
      - "contracts/module-contract-bindings.json"
      - "modules/*/module.yaml"
      - "schemas/module.schema.json"
      - "scripts/contract_bindings.py"
      - "scripts/generate_contract_bindings.py"
      - ".github/workflows/product-plane.yml"
      - "scripts/run_e2e.sh"
      - "proto/**"
      - "crates/crm-application-runtime/**"
      - "crates/crm-proto-contracts/**"
      - "services/crm-api/**"
      - "database/**"

# Only the newest Product Plane validation for a PR/ref is useful. Cancelling stale runs
# prevents old exact-SHA candidates from consuming hosted runners while the current head waits.
concurrency:
  group: product-plane-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  check-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v7

      - uses: dtolnay/rust-toolchain@stable

      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: requirements-dev.txt

      - name: Install contract generation dependencies
        run: python -m pip install --requirement requirements-dev.txt

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24.18.0"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: "11.12.0"

      - name: Install Node dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify generated contract artifacts are up-to-date
        run: |
          pnpm web:generate
          git diff --exit-code contracts/module-contract-bindings.json packages/client/src/contract_hashes.ts

      - name: Run Web typecheck
        run: pnpm web:typecheck

      - name: Run Web lint
        run: pnpm web:lint

      - name: Run Web unit tests
        run: |
          set -o pipefail
          pnpm web:test 2>&1 | tee product-plane-unit.log

      - name: Run Web build
        run: pnpm web:build

      - name: Install PostgreSQL client
        run: |
          sudo apt-get update
          sudo apt-get install --yes postgresql-client

      - name: Run E2E Integration Suite
        run: ./scripts/run_e2e.sh 2>&1 | tee product-plane-e2e.log

      - name: Upload Product Plane diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: product-plane-diagnostics
          path: |
            contracts/module-contract-bindings.json
            packages/client/src/contract_hashes.ts
            product-plane-unit.log
            product-plane-e2e.log
            apps/web/playwright-report
            apps/web/test-results
          if-no-files-found: ignore
          retention-days: 7
