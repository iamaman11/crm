name: Agent Exact Architecture Apply

on:
  pull_request:
    paths:
      - ".github/workflows/agent-exact-architecture.yml"

permissions:
  contents: write

concurrency:
  group: agent-exact-architecture-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  apply:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - name: Apply exact source architecture delta
        id: packet
        shell: bash
        run: |
          if ! compgen -G '.agent/exact-existing.patch.part.*' >/dev/null; then
            echo "apply=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          cat .agent/exact-existing.patch.part.* > /tmp/exact-existing.patch
          echo "27c46dc0bbb911b53524158909fbafefd2dd5abe1981de261bf15c34e14f1139  /tmp/exact-existing.patch" | sha256sum --check
          git apply --exclude=.github/workflows/governance.yml --check /tmp/exact-existing.patch
          git apply --exclude=.github/workflows/governance.yml --index /tmp/exact-existing.patch
          git rm .agent/exact-existing.patch.part.*
          git rm -f .agent/final-code.patch.part.* .agent/final-code.patch.ready
          echo "apply=true" >> "$GITHUB_OUTPUT"
      - name: Publish final source architecture delta
        if: steps.packet.outputs.apply == 'true'
        env:
          HEAD_BRANCH: ${{ github.head_ref }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git commit -m "Complete lifecycle parity and module scaffolding gates"
          git push origin "HEAD:${HEAD_BRANCH}"
