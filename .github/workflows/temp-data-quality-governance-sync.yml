name: Temporary Data Quality Governance Sync

on:
  push:
    branches:
      - docs/data-quality-privacy-accepted
    paths:
      - ".github/workflows/temp-data-quality-governance-sync.yml"

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: docs/data-quality-privacy-accepted
          fetch-depth: 0
      - name: Synchronize authoritative privacy owner state
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          import re

          SOURCE = "dcfe8faebc7462b888f8fc1721cb379a40fea88a"
          MERGE = "deac197c97cddc15bb9916092ca87f6e767ce1de"

          def read(path):
              return Path(path).read_text(encoding="utf-8")

          def write(path, text):
              Path(path).write_text(text, encoding="utf-8")

          def replace_once(text, old, new, label):
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{label}: expected one match, found {count}")
              return text.replace(old, new)

          # PROJECT_STATUS.md
          path = "docs/PROJECT_STATUS.md"
          text = read(path)
          text = replace_once(
              text,
              "9. `CUSTOMER_DATA_OPERATIONS_PRIVACY_SCOPE_PACKET.md` — accepted historical contract for the seventh owner contribution.",
              "9. `CUSTOMER_DATA_OPERATIONS_PRIVACY_SCOPE_PACKET.md` — accepted historical contract for the seventh owner contribution.\n10. `DATA_QUALITY_PRIVACY_SCOPE_PACKET.md` — accepted historical contract for the eighth owner contribution.",
              "project references",
          )
          text = replace_once(
              text,
              "Nine owner-scope contribution coordinates are published and remain contract-only/non-runtime. Seven authoritative owner implementations are accepted:",
              "Nine owner-scope contribution coordinates are published and remain contract-only/non-runtime. Eight authoritative owner implementations are accepted:",
              "project owner count",
          )
          text = replace_once(
              text,
              "7. Customer Data Operations — PR #188 / accepted source `07f34786e82fdfa78d263790e9f50541529006f8` / merge `089be72fa3010b4aa15aff7f9ea55fd86290f8fc` / 26 of 26 permanent workflows.",
              "7. Customer Data Operations — PR #188 / accepted source `07f34786e82fdfa78d263790e9f50541529006f8` / merge `089be72fa3010b4aa15aff7f9ea55fd86290f8fc` / 26 of 26 permanent workflows.\n8. Data Quality — PR #190 / accepted source `" + SOURCE + "` / merge `" + MERGE + "` / 27 of 27 permanent workflows.",
              "project Data Quality entry",
          )
          text = replace_once(
              text,
              "Customer Data Operations remains contract-only/non-runtime. Its exact-head gate proves strict four-family persistence rehydration, bounded keyset scans, alias-aware canonical Party resolution, exact selection-to-stage/outcome association, deterministic heterogeneous pagination, reference-only response bytes, no query-side writes, clean PostgreSQL, complete rollback and schema removal, reapply, repeated acceptance and no regression in the shared Identity Resolution topology proof.",
              "Customer Data Operations remains contract-only/non-runtime. Its exact-head gate proves strict four-family persistence rehydration, bounded keyset scans, alias-aware canonical Party resolution, exact selection-to-stage/outcome association, deterministic heterogeneous pagination, reference-only response bytes, no query-side writes, clean PostgreSQL, complete rollback and schema removal, reapply, repeated acceptance and no regression in the shared Identity Resolution topology proof.\n\nData Quality also remains contract-only/non-runtime. Its accepted gate proves strict nine-type owner rehydration, exclusion of shared rule/profile definitions, exact seven-family Party evidence, alias-aware relevance, rule/profile/job/input/outcome/finding/observation/completeness/remediation association integrity, deterministic pagination, minimized reference-only bytes, primary-key access-path proof, zero writes and clean/rollback/reapply PostgreSQL acceptance.",
              "project accepted evidence",
          )
          text = replace_once(
              text,
              "Data Quality privacy owner contribution\n-> Customer Enrichment privacy owner contribution",
              "Customer Enrichment privacy owner contribution",
              "project active lane",
          )
          text = replace_once(
              text,
              "Only one owner implementation is active at a time. Production scope discovery remains forbidden until all nine owner contributions are accepted.",
              "Only one owner implementation remains. Production scope discovery remains forbidden until all nine owner contributions are accepted.",
              "project owner remaining",
          )
          replacement = '''## Next bounded packet — Customer Enrichment

Coordinate: `customer_enrichment.privacy.scope.contribute@1.0.0`  
State: **Inspection and bounded-entry freeze next; implementation not yet accepted**

The entry packet must resolve and freeze:

- which of the nine persisted Enrichment record families contain authoritative Party-subject evidence;
- shared provider-profile and mapping definitions that must remain excluded from one Party contribution;
- request, provider-response/conflict, suggestion, review, application-attempt and usage lineage;
- direct Party references, parent-record associations and any bounded provenance-based relevance;
- provider payload, suggestion values, review reasons, usage details, secret/provider identifiers and other private evidence excluded from response bytes;
- alias-aware relevance under the accepted Identity Resolution topology;
- deterministic family ordering, keyset pagination, strict rehydration and owner-wide bounds;
- PostgreSQL access-path/index requirements and no-write proof;
- contract-only delivery without public ingress, application registration or worker reachability.

Customer Enrichment is the ninth and final owner packet before production discovery and immutable planning.

'''
          text, count = re.subn(
              r"## Next bounded packet — Data Quality\n.*?(?=## Remaining product work)",
              replacement,
              text,
              flags=re.S,
          )
          if count != 1:
              raise SystemExit(f"project next section: expected one match, found {count}")
          write(path, text)

          # MODULE_CATALOG.md
          path = "docs/MODULE_CATALOG.md"
          text = read(path)
          text = replace_once(
              text,
              "Nine owner-scope contribution coordinates are published and remain contract-only/non-runtime. Seven authoritative implementations are accepted:",
              "Nine owner-scope contribution coordinates are published and remain contract-only/non-runtime. Eight authoritative implementations are accepted:",
              "catalog owner count",
          )
          text = replace_once(
              text,
              "- PR #188 — Customer Data Operations, strict import/export evidence rehydration, bounded alias-safe scans and exact selection-to-stage/outcome association; accepted source `07f34786e82fdfa78d263790e9f50541529006f8`, merge `089be72fa3010b4aa15aff7f9ea55fd86290f8fc`, 26 of 26 permanent workflows.",
              "- PR #188 — Customer Data Operations, strict import/export evidence rehydration, bounded alias-safe scans and exact selection-to-stage/outcome association; accepted source `07f34786e82fdfa78d263790e9f50541529006f8`, merge `089be72fa3010b4aa15aff7f9ea55fd86290f8fc`, 26 of 26 permanent workflows.\n- PR #190 — Data Quality, strict nine-type rehydration, shared-definition exclusion, exact seven-family Party evidence and cross-record lineage validation; accepted source `" + SOURCE + "`, merge `" + MERGE + "`, 27 of 27 permanent workflows.",
              "catalog Data Quality entry",
          )
          text = replace_once(text, "All seven remain non-runtime", "All eight remain non-runtime", "catalog non-runtime count")
          text = replace_once(
              text,
              "Data Quality is the next bounded contract-only owner. Customer Enrichment follows as the final owner contribution before production scope discovery/planning.",
              "Customer Enrichment is the next bounded contract-only owner and the final owner contribution before production scope discovery/planning.",
              "catalog next owner",
          )
          text = replace_once(
              text,
              "The accepted Customer Data Operations packet distinguishes subject-level import-row/export-selection/execution evidence from multi-subject job, progress and artifact containers. It is now an historical contract and introduces no container-level deletion semantics.",
              "The accepted Customer Data Operations packet distinguishes subject-level import-row/export-selection/execution evidence from multi-subject job, progress and artifact containers. It is now an historical contract and introduces no container-level deletion semantics.\n\nThe accepted Data Quality packet contributes only evaluation job/input, rule outcome, finding/observation, completeness-result and remediation-attempt references. Shared rule-set/profile definitions remain owner validation dependencies and are not Party evidence. The packet introduces no query-side writes or runtime promotion.",
              "catalog accepted packet",
          )
          text = replace_once(
              text,
              "Seven of nine owner contributions are accepted through PR #188. Data Quality is next; Customer Enrichment remains the final owner before discovery/planning.",
              "Eight of nine owner contributions are accepted through PR #190. Customer Enrichment is the ninth and final owner before discovery/planning.",
              "catalog accounting",
          )
          write(path, text)

          # IMPLEMENTATION_ROADMAP.md
          path = "docs/IMPLEMENTATION_ROADMAP.md"
          text = read(path)
          text = replace_once(
              text,
              "7. Customer Data Operations — PR #188 / accepted source `07f34786e82fdfa78d263790e9f50541529006f8` / merge `089be72fa3010b4aa15aff7f9ea55fd86290f8fc` / 26 of 26 permanent workflows.",
              "7. Customer Data Operations — PR #188 / accepted source `07f34786e82fdfa78d263790e9f50541529006f8` / merge `089be72fa3010b4aa15aff7f9ea55fd86290f8fc` / 26 of 26 permanent workflows.\n8. Data Quality — PR #190 / accepted source `" + SOURCE + "` / merge `" + MERGE + "` / 27 of 27 permanent workflows.",
              "roadmap Data Quality entry",
          )
          old_sequence = '''1. **Data Quality owner slice — inspection/entry freeze next:** define authoritative record families, Party relevance, shared-container exclusions, retention/evidence semantics, pagination and frozen bounds before implementation.
2. **Data Quality owner implementation:** implement only after the bounded entry packet removes record-family ambiguity.
3. **Customer Enrichment owner slice:** freeze and implement provider/provenance/review/application evidence scope.
4. **Complete nine-owner set:** require all owner gates before production discovery.'''
          new_sequence = '''1. **Customer Enrichment owner slice — inspection/entry freeze next:** define authoritative record families, Party relevance, shared-definition/container exclusions, provider/provenance/review/application evidence, pagination and frozen bounds before implementation.
2. **Customer Enrichment owner implementation:** complete the ninth and final owner without runtime promotion.
3. **Complete nine-owner set:** require all owner gates before production discovery.'''
          text = replace_once(text, old_sequence, new_sequence, "roadmap active sequence")
          # Renumber the remaining active sequence after removal of one item.
          for old, new in [("5. **Scope discovery", "4. **Scope discovery"), ("6. **Deterministic planning", "5. **Deterministic planning"), ("7. **Approval", "6. **Approval"), ("8. **Legal hold", "7. **Legal hold"), ("9. **Resumable execution", "8. **Resumable execution"), ("10. **Access/export", "9. **Access/export"), ("11. **Tombstone", "10. **Tombstone"), ("12. **Worker", "11. **Worker"), ("13. **Phase 8A closure", "12. **Phase 8A closure"), ("14. **Phase 8B", "13. **Phase 8B")]:
              text = replace_once(text, old, new, f"roadmap renumber {old}")
          replacement = '''### 6.4 Accepted historical owner slice — Data Quality

PR #190 accepted seven direct-subject families:

- `data_quality.party_evaluation_job`;
- `data_quality.party_evaluation_input`;
- `data_quality.rule_outcome`;
- `data_quality.finding`;
- `data_quality.finding_observation`;
- `data_quality.party_completeness_result`;
- `data_quality.remediation_attempt`.

The accepted packet proves strict existing-owner rehydration for all nine persisted types, exclusion of shared rule-set/profile definitions, alias-aware Party relevance, exact cross-record lineage, bounded keyset scans, deterministic pagination, minimized reference-only evidence, primary-key access-path proof, zero writes and clean/rollback/schema-removal/reapply PostgreSQL acceptance. The coordinate remains non-runtime.

### 6.5 Next bounded owner slice — Customer Enrichment

Before adapter implementation, freeze:

- the authoritative provider-profile, mapping, request, provider-response/conflict, suggestion, review, application-attempt and usage record families;
- direct Party references and bounded parent/provenance associations that constitute subject relevance;
- shared definitions and multi-subject/provider containers excluded from one Party scope;
- exact owner decoders and malformed-state fail-closed behavior;
- alias-aware relevance under the accepted topology generation;
- deterministic cross-family order, keyset scans, raw-row counters and terminal `page_size + 1` proof;
- owner-wide scan, rehydration, association and canonical-resolution bounds;
- provider payload, suggestion/review/application/usage privacy and minimized reference-only evidence;
- required PostgreSQL indexes and clean rollback/reapply acceptance;
- contract-only implementation with no public route, application registration, new worker or runtime promotion.

'''
          text, count = re.subn(
              r"### 6\.4 Next bounded owner slice — Data Quality\n.*?(?=## 7\.)",
              replacement,
              text,
              flags=re.S,
          )
          if count != 1:
              raise SystemExit(f"roadmap owner section: expected one match, found {count}")
          write(path, text)

          # PHASE8_DELIVERY_PLAN.md
          path = "docs/PHASE8_DELIVERY_PLAN.md"
          text = read(path)
          text = replace_once(
              text,
              "7. Customer Data Operations — PR #188 / source `07f34786e82fdfa78d263790e9f50541529006f8` / merge `089be72fa3010b4aa15aff7f9ea55fd86290f8fc` / 26 of 26 permanent workflows.",
              "7. Customer Data Operations — PR #188 / source `07f34786e82fdfa78d263790e9f50541529006f8` / merge `089be72fa3010b4aa15aff7f9ea55fd86290f8fc` / 26 of 26 permanent workflows.\n8. Data Quality — PR #190 / source `" + SOURCE + "` / merge `" + MERGE + "` / 27 of 27 permanent workflows.",
              "phase Data Quality entry",
          )
          text = replace_once(
              text,
              "Customer Data Operations remains non-runtime. Its accepted gate proves bounded four-family owner discovery, strict persisted-contract rehydration, alias-aware canonical Party relevance, exact selection-to-stage/outcome association, deterministic pagination, reference-only output, no writes, clean PostgreSQL, complete rollback/schema removal, reapply and repeated acceptance. The shared Identity Resolution read-only snapshot topology path also passed its permanent workflow on the same exact head.",
              "Customer Data Operations remains non-runtime. Its accepted gate proves bounded four-family owner discovery, strict persisted-contract rehydration, alias-aware canonical Party relevance, exact selection-to-stage/outcome association, deterministic pagination, reference-only output, no writes, clean PostgreSQL, complete rollback/schema removal, reapply and repeated acceptance. The shared Identity Resolution read-only snapshot topology path also passed its permanent workflow on the same exact head.\n\nData Quality remains non-runtime. Its accepted gate proves strict nine-type rehydration, exclusion of shared definitions, seven-family Party evidence, exact association integrity, alias-aware relevance, deterministic pagination, minimized output, no writes, primary-key access-path proof and repeated clean PostgreSQL acceptance.",
              "phase accepted evidence",
          )
          replacement = '''### Next bounded owner packet: Customer Enrichment

Coordinate: `customer_enrichment.privacy.scope.contribute@1.0.0`  
State: **Inspection and bounded-entry freeze next; adapter implementation is not yet accepted**

The packet must inspect and freeze:

- authoritative Enrichment record families and strict persisted contracts;
- Party relevance across requests, responses/conflicts, suggestions, reviews, application attempts and provider usage;
- shared provider-profile/mapping definitions and other containers excluded from one subject;
- direct Party references and bounded parent/provenance associations;
- provider payload, suggestion values, review reasons, application details, usage details and provider/secret identifiers excluded from response bytes;
- alias-aware relevance under the accepted topology generation;
- deterministic family order, same-tenant keyset pagination and terminal `page_size + 1` proof;
- raw scanned-row accounting and owner-wide scan/rehydration/association/canonical-resolution bounds;
- PostgreSQL access-path/index requirements, no-write proof, rollback and reapply;
- contract-only implementation with no route, application composition, new worker or runtime promotion.

Implementation must not start until ambiguous record-family or subject-relevance semantics are resolved in the entry packet.

### Following owner packets

1. Customer Enrichment privacy-scope implementation after entry freeze;
2. complete nine-owner set.

Only one owner packet is active at a time.

'''
          text, count = re.subn(
              r"### Next bounded owner packet: Data Quality\n.*?(?=### Accepted historical packet: Customer Data Operations)",
              replacement,
              text,
              flags=re.S,
          )
          if count != 1:
              raise SystemExit(f"phase current owner section: expected one match, found {count}")
          marker = "Import/export jobs, selection boundaries/progress, complete artifacts and other multi-subject containers remain excluded from automatic Party scope.\n"
          addition = marker + '''
### Accepted historical packet: Data Quality

`DATA_QUALITY_PRIVACY_SCOPE_PACKET.md` is an accepted historical contract for PR #190. It contributes only seven direct Party-bearing families while shared rule-set/profile definitions remain strict owner validation dependencies and are excluded from subject evidence.

The implementation remains contract-only/non-runtime and introduces no query-side writes, public route, application composition or Customer Privacy worker.
'''
          text = replace_once(text, marker, addition, "phase Data Quality historical packet")
          write(path, text)

          # DATA_QUALITY_PRIVACY_SCOPE_PACKET.md
          path = "docs/DATA_QUALITY_PRIVACY_SCOPE_PACKET.md"
          text = read(path)
          text = replace_once(
              text,
              "Status: **Bounded entry contract frozen; implementation not started**  \nParent program: #126  \nCoordinate: `data_quality.privacy.scope.contribute@1.0.0`  \nBaseline: `3a7648824670b6f2d7c7d99b857245e9cc332733`  \nRuntime state: **Contract-only/non-runtime; no route, worker or application registration is permitted in this packet**",
              "Status: **Accepted historical contract**  \nParent program: #126  \nCoordinate: `data_quality.privacy.scope.contribute@1.0.0`  \nAccepted source: `" + SOURCE + "`  \nMerge: `" + MERGE + "`  \nExact-head gate: **27 of 27 permanent workflows succeeded**  \nRuntime state: **Contract-only/non-runtime; no route, worker or application registration was added**",
              "packet status",
          )
          text = replace_once(
              text,
              "This entry packet freezes the persisted record families, Party relevance, association validation, strict rehydration, scan and rehydration bounds, pagination order, response minimization, PostgreSQL access path and acceptance boundary before adapter code is written.",
              "The accepted implementation preserves the frozen persisted record families, Party relevance, association validation, strict rehydration, scan and rehydration bounds, pagination order, response minimization, PostgreSQL access path and acceptance boundary.",
              "packet objective state",
          )
          text = replace_once(
              text,
              "Add a permanent `Data Quality Privacy Scope CI` only with implementation code. It must prove:",
              "The permanent `Data Quality Privacy Scope CI` was added with implementation code and proves:",
              "packet workflow tense",
          )
          text = replace_once(
              text,
              "The workflow must remain permanent after merge.",
              "The workflow remains permanent after merge.",
              "packet permanent tense",
          )
          text = replace_once(
              text,
              "The persisted record-family and subject-relevance semantics are sufficiently unambiguous to begin a contract-only adapter after this packet's governance checks pass.\n\nImplementation remains not started in this commit. The next source change may add the dedicated Data Quality privacy adapter and its permanent workflow only while preserving every frozen boundary above. Customer Enrichment remains the final owner after Data Quality. Production discovery remains forbidden until all nine owner contributions are accepted.",
              "The persisted record-family and subject-relevance semantics were implemented without weakening the frozen contract. PR #190 accepted the dedicated Data Quality privacy adapter and permanent workflow on source `" + SOURCE + "`, with 27 of 27 permanent workflows successful before squash merge `" + MERGE + "`.\n\nThis packet is now an accepted historical contract. Customer Enrichment is the ninth and final owner. Production discovery remains forbidden until that final owner contribution is accepted.",
              "packet conclusion",
          )
          write(path, text)

          # Governance regression test.
          path = "tests/test_customer_privacy_owner_scope_contracts.py"
          text = read(path)
          text = replace_once(
              text,
              "def test_status_sources_freeze_seven_accepted_owners_and_data_quality_next(self) -> None:",
              "def test_status_sources_freeze_eight_accepted_owners_and_customer_enrichment_next(self) -> None:",
              "test name",
          )
          text = replace_once(
              text,
              '''        customer_data_packet = (
            ROOT / "docs/CUSTOMER_DATA_OPERATIONS_PRIVACY_SCOPE_PACKET.md"
        ).read_text(encoding="utf-8")
''',
              '''        customer_data_packet = (
            ROOT / "docs/CUSTOMER_DATA_OPERATIONS_PRIVACY_SCOPE_PACKET.md"
        ).read_text(encoding="utf-8")
        data_quality_packet = (
            ROOT / "docs/DATA_QUALITY_PRIVACY_SCOPE_PACKET.md"
        ).read_text(encoding="utf-8")
''',
              "test packet read",
          )
          text = replace_once(
              text,
              '''        self.assertIn("Seven authoritative owner implementations are accepted", project_status)
        self.assertIn("Seven authoritative implementations are accepted", module_catalog)
        self.assertIn("Next bounded packet — Data Quality", project_status)
        self.assertIn("Data Quality is the next bounded contract-only owner", module_catalog)
        self.assertIn("Next bounded owner slice — Data Quality", roadmap)
        self.assertIn("Next bounded owner packet: Data Quality", phase_plan)
''',
              '''        self.assertIn("Eight authoritative owner implementations are accepted", project_status)
        self.assertIn("Eight authoritative implementations are accepted", module_catalog)
        self.assertIn("Next bounded packet — Customer Enrichment", project_status)
        self.assertIn("Customer Enrichment is the next bounded contract-only owner", module_catalog)
        self.assertIn("Next bounded owner slice — Customer Enrichment", roadmap)
        self.assertIn("Next bounded owner packet: Customer Enrichment", phase_plan)
''',
              "test current state assertions",
          )
          anchor = '''        self.assertIn("Production discovery remains prohibited", customer_data_packet)
'''
          addition = anchor + '''
        for document in (project_status, module_catalog, roadmap, phase_plan, data_quality_packet):
            self.assertIn("PR #190", document)
            self.assertIn("''' + SOURCE + '''", document)
            self.assertIn("''' + MERGE + '''", document)

        self.assertIn("27 of 27 permanent workflows", project_status)
        self.assertIn("27 of 27 permanent workflows", module_catalog)
        self.assertIn("27 of 27 permanent workflows", roadmap)
        self.assertIn("27 of 27 permanent workflows", phase_plan)
        self.assertIn("27 of 27 permanent workflows succeeded", data_quality_packet)
        self.assertIn("Accepted historical contract", data_quality_packet)
        self.assertIn("Production discovery remains forbidden", data_quality_packet)
'''
          text = replace_once(text, anchor, addition, "test Data Quality evidence")
          text = replace_once(
              text,
              '''            "Six authoritative owner implementations are accepted",
            "Six authoritative implementations are accepted",
            "Customer Data Operations is the next bounded contract-only owner",
            "Next bounded owner slice — Customer Data Operations",
            "Next bounded owner packet: Customer Data Operations",
            "Ready; implementation not started",
            "implementation code has not started",
            "Implementation state: **Not started",
            "PR #188 in progress",
''',
              '''            "Six authoritative owner implementations are accepted",
            "Six authoritative implementations are accepted",
            "Seven authoritative owner implementations are accepted",
            "Seven authoritative implementations are accepted",
            "Customer Data Operations is the next bounded contract-only owner",
            "Data Quality is the next bounded contract-only owner",
            "Next bounded owner slice — Customer Data Operations",
            "Next bounded owner slice — Data Quality",
            "Next bounded owner packet: Customer Data Operations",
            "Next bounded owner packet: Data Quality",
            "Ready; implementation not started",
            "implementation code has not started",
            "Implementation state: **Not started",
            "PR #188 in progress",
            "PR #190 in progress",
''',
              "test stale claims",
          )
          text = replace_once(
              text,
              '''            "Contract-only/non-runtime",
            "implementation not started",
            "Production discovery remains forbidden",
''',
              '''            "Contract-only/non-runtime",
            "Accepted historical contract",
            "27 of 27 permanent workflows succeeded",
            "''' + SOURCE + '''",
            "''' + MERGE + '''",
            "Production discovery remains forbidden",
''',
              "test packet accepted controls",
          )
          write(path, text)

          Path(".github/workflows/temp-data-quality-governance-sync.yml").unlink()
          PY
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs(privacy): accept Data Quality owner contribution"
          git push origin HEAD:docs/data-quality-privacy-accepted
